1. Want to make sure the change-function overlap is in the same file?
	- Refactorings? What if a function was moved to a different file 
	  after spending 90% of its life in another?

2. Other history. A commit from 2 years ago will have different function locations
   than one from today. We run the lua script on 
		- the raw "new" file (no annotations)
			- For each commit.
			- Score each occurence with a time decay
			- 
3. Efficiency. Doing a continuous git log -p would probably be more efficient than 
   piece by piece git shows. Is this possible?
	- Regex to extract Author: (email)
	  Date: 
	  File
	  


// ----------- 03/23/26 -----------------

Author,

Author email?

Date: Age of commit

key, value {function_name, # of lines modified, # of times called?}
	- Added lines or lines changed
		- Potentially weigh/score later
	
Keep track of function calls?
	- If a person calls a function a lot they are more likely to know how it works.
	
		
Can attach | to commands to feed into program.


Go through all commits, get line changes, 

Find associated functions of changes (what function body are the changes in) 

Same for lovely injector patches. 

Get list of recommendors
	- Counting occurences
	- Time decay
	- Lovely vs main code for patches
	- Dev scoring function
		

Get all the commits for an author. See 
